# CableTrack Routing Tool — Prototype

A prototype sea-route planning tool: pick a start point and destination, get an automatic land-avoiding sea route, drag it to correct it, and see transit time, ETA allowance, fuel and cost broken down by speed zone.

Deployed to Render for evaluation. See backend/ (FastAPI) and static/ (Leaflet frontend).
